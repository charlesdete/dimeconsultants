# Dockerfile.prod
FROM node:24-alpine

WORKDIR /app

COPY package.json package-lock.json ./

# Use ci for clean reproducible install
RUN npm ci --omit=dev

COPY . .

RUN npm run build

EXPOSE 3030

CMD ["npm", "run", "prod"]