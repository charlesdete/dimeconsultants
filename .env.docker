# Docker environment configuration
# For development with Docker (optional override)

NODE_ENV=development

# SMTP Configuration - uses Mailhog for local testing
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
RECIPIENT_EMAIL=hello@dimeconsultants.co.ke
