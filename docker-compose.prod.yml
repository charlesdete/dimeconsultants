# docker-compose.prod.yml
version: "3.8"

services:
  app:
    image: ghcr.io/charlesdete/ai-navigator-frontend:latest # pulled from GHCR by CI/CD
    container_name: dime-ai-navigator
    ports:
      - "3030:3030"
    environment:
      - NODE_ENV=production
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - RECIPIENT_EMAIL=${RECIPIENT_EMAIL}
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3030/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - dime-network

  nginx:
    image: nginx:alpine
    container_name: dime-nginx
    ports:
      - "8080:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro # SSL certs from certbot
      - /var/www/certbot:/var/www/certbot:ro # ACME challenge
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - dime-network

networks:
  dime-network:
    driver: bridge
