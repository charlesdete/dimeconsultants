import { createFileRoute } from "@tanstack/react-router";
import { PageLayout } from "@/components/PageLayout";
import { Section, Eyebrow } from "@/components/Section";
import { useState } from "react";
import { ArrowRight, RotateCcw } from "lucide-react";
export const Route = createFileRoute("/scanner")({ head: () => ({ meta: [{ title: "AI Readiness Scanner | Dime Consultants" }, { name: "description", content: "Take our 2-minute AI Readiness Scanner and get an instant score plus tailored recommendations." }] }), component: ScannerPage });
const QUESTIONS = [{ q: "Does your organisation have a documented AI strategy?", o: ["No", "In progress", "Yes — published"] }, { q: "What share of your team uses AI tools weekly?", o: ["<10%", "10–40%", "40%+"] }, { q: "Do you have an AI governance or ethics policy?", o: ["No", "Drafting", "Approved & live"] }, { q: "Are AI initiatives tied to measurable KPIs?", o: ["Rarely", "Sometimes", "Always"] }, { q: "How mature is your data foundation for AI?", o: ["Fragmented", "Workable", "Strong"] }, { q: "Do leaders sponsor AI transformation actively?", o: ["No", "Some", "Yes, top-down"] }];
function ScannerPage() { const [answers, setAnswers] = useState([]); const [done, setDone] = useState(false); const step = answers.length; const score = answers.reduce((a, b) => a + b, 0); const pct = Math.round((score / (QUESTIONS.length * 2)) * 100); const tier = pct >= 75 ? { name: "AI Accelerator", note: "You're ahead of the curve. Time to scale agentic automations across the enterprise." } : pct >= 45 ? { name: "AI Builder", note: "Strong foundations. A structured 90-day programme will compound your gains." } : { name: "AI Starter", note: "Big upside ahead. Start with foundations, governance and a focused pilot." }; const reset = () => { setAnswers([]); setDone(false); }; return <PageLayout><Section><Eyebrow>Interactive Tool</Eyebrow><h1 className="page-title">AI Readiness Scanner</h1><p className="page-copy">Answer 6 quick questions. Get an instant readiness tier and recommended next steps.</p><div className="scanner-panel">{!done ? <><div className="scanner-status"><span>Question {Math.min(step + 1, QUESTIONS.length)} of {QUESTIONS.length}</span><span>{Math.round((step / QUESTIONS.length) * 100)}%</span></div><div className="progress-track"><div className="progress-fill" style={{ width: `${(step / QUESTIONS.length) * 100}%` }} /></div><h3 className="scanner-question">{QUESTIONS[step].q}</h3><div className="answer-grid">{QUESTIONS[step].o.map((o, i) => <button key={o} onClick={() => { const next = [...answers, i]; setAnswers(next); if (next.length === QUESTIONS.length) setDone(true); }} className="answer-button"><span>{o}</span><ArrowRight size={16} /></button>)}</div></> : <div className="scanner-result"><div className="result-label">Your readiness tier</div><div className="result-tier">{tier.name}</div><div className="result-score">{pct}/100</div><p className="result-note">{tier.note}</p><div className="result-actions"><a href="/contact" className="button-primary">Discuss your results <ArrowRight size={16} /></a><button onClick={reset} className="button-secondary"><RotateCcw size={14} />Retake</button></div></div>}</div></Section></PageLayout>; }
